#include <iostream>
#include <math.h>
using namespace std;
int main()
{
    double a, b, c;
    if (a < 0)
    {
        a = abs(a);
    }
    if (b < 0)
    {
        b = abs(b);
    }
    if (c < 0)
    {
        c = abs(c);
    }
}